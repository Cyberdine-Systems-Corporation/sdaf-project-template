# Escenario default-core. Cambia project.name.
# Catálogo: sdaf-core/sdaf.config.schema.yaml y sdaf-core/examples/
# Pack opcional: deja null; para .NET ver sdaf-stack-dotnet@0.1.0 (ADOPT del pack).

sdaf:
  version: "0.2.1"

project:
  name: MiProyecto
  language: es

stack:
  pack: null
  src_path: src
  tests_path: tests

agents:
  active:
    - specification
    - architecture
    - testing-review
  stubs:
    - product
    - domain
    - application
    - devops
    - review
    - testing
  fusions: {}
